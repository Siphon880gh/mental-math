# LOOP — Shortcuts (retired Archive)

Archive is **not** a learner surface. Literacy is **Guides** only (`/guides`), with Track A and Track B. `/archive` redirects to `/guides`.

Shortcuts (rule + example) live on `/guides/:slug`, stored as `Trick` rows in `src/lib/tricks.ts`.

Do **not** run this file as a graph node. The graph has six nodes: Guides → Coach → Drills → Games → Cases → Home.

If a family still has 0 shortcut rows, add the row in the **same Guides tick** that authors or deepens that guide (`LOOP-Guides.md`). Extra shortcut rows for a family already on the hub are optional later depth — still not a separate nav.

```text
Do not: /loop exhaustive Archive content using LOOPS/LOOP-Archive.md
Do:     /loop exhaustive Guides content using LOOPS/LOOP-Guides.md
```
